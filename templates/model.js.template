// Load module dependencies
var mongoose = require('mongoose');

{{relatedModels}}

// Define {{modelName}} attributes
var {{modelName}}Schema = mongoose.Schema({
{{modelSchema}}
});

// Export {{modelName}} model
module.exports = mongoose.model('{{modelName}}', {{modelName}}Schema);
